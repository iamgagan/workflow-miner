{
  "name": "@workflow-miner/desktop",
  "version": "0.1.0-alpha.13",
  "private": true,
  "description": "Workflow Miner macOS desktop app — Tauri shell wrapping the Next.js dashboard with a local PGlite brain.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:universal": "tauri build --target universal-apple-darwin",
    "tauri": "tauri",
    "next:dev": "node scripts/run-next.mjs --dev",
    "next:start": "node scripts/run-next.mjs",
    "doctor": "node scripts/doctor.mjs"
  },
  "dependencies": {
    "@workflow-miner/engine": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
